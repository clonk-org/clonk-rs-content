map Groar {
  /* Definitionen */
  overlay Mat {
    algo = bozo;
    sub = 1;
    a = 30;
    turbulence = 5000;
    zoomX = 40;
    zoomY = 20;
  };

  overlay AlterMat {
    sub = 1;
    turbulence = 10;
    algo = rndchecker;
    zoomX = -70;
    zoomY = -70;
  };

  /* Verbindungen zwischen Himmelsinseln */
  overlay {
    mat = Tunnel;
    tex = Smooth;
    algo = rndchecker;
    zoomX = 20;
    zoomY = 20;
    turbulence = 5000;
  } & Mat {
    mat = Ice;
    algo = lines;
    tex = Flare;
    zoomX = 40;
    zoomY = 40;
    a = 1;
    b = 20;
    sub = 0;
    rotate = 90;

    // Schnee
    AlterMat {
      mat = Snow; tex = Smooth; sub = 0; a = 3; sub = 0;
      AlterMat { mat = Snow; tex = Spots; a = 4; sub = 0; };
      AlterMat { mat = Snow; tex = Spots2; a = 4; sub = 0; };
      AlterMat { mat = Snow; tex = Sponge; a = 4; sub = 0; };
    };

    // Eis
    AlterMat {
      mat = Ice; tex = Structure; a = 3; sub = 0;
      AlterMat { mat = Ice; tex = Ridge; a = 4; sub = 0; };
      AlterMat { mat = Ice; tex = Flare2; a = 4; sub = 0; };
      AlterMat { mat = Ice; tex = Flare3; a = 4; sub = 0; };
    };
  };

  /* Himmelsinseln */
  overlay {
    grp = 1;
    mask = 1;
    turbulence = 10000;

    // Kristallinseln
    Mat {
      sub = 0;
      a = 30;
      mat = Crystal;
      tex = Smooth;
      zoomX = 10;
      zoomY = 10;
      AlterMat {
        mat = Crystal; tex = flare; a = 5; sub = 0;
        AlterMat { mat = Crystal; tex = Structure; a = 2; sub = 0; };
      };
    };

    // Schneeinseln
    Mat {
      sub = 0;
      a = 20;
      mat = Snow;
      tex = Smooth;
      zoomX = 10;
      zoomY = 10;
      AlterMat { mat = Snow; tex = Spots; a = 4; sub = 0; };
      AlterMat { mat = Snow; tex = Spots2; a = 4; sub = 0; };
      AlterMat { mat = Snow; tex = Sponge; a = 4; sub = 0; };
    };

    // Normale Erdhimmelsinseln
    Mat {
      a = 10;
      mat = Earth;
      tex = Smooth2;
      AlterMat { mat = Earth; tex = Sponge; a = 4; };
      AlterMat { mat = Earth; tex = Structure2; a = 4; };
      AlterMat { mat = Earth; tex = Smooth3; a = 4; };

      // Kristall
      AlterMat {
        mat = Crystal; tex = Flare; a = 4;
        AlterMat { mat = Crystal; tex = Structure; a = 2; };
      };

      // Eis
      AlterMat {
        mat = Ice; tex = Structure; a = 3;
        AlterMat { mat = Ice; tex = Ridge; a = 4; };
        AlterMat { mat = Ice; tex = Flare2; a = 4; };
        AlterMat { mat = Ice; tex = Flare3; a = 4; };
      };

      // Schnee
      AlterMat {
        mat = Snow; tex = Smooth; a = 2;
        zoomX = -80; zoomY = -80;
        AlterMat { mat = Snow; tex = Spots; a = 4; };
        AlterMat { mat = Snow; tex = Spots2; a = 4; };
        AlterMat { mat = Snow; tex = Sponge; a = 4; };
      };

      // Rahmen um die Inseln drumrum
      overlay { algo=border; a=1; b=1; mat=Earth; tex=Smooth; };
    };
  };

  overlay Sky {
    // muss etwas über die Landschaft überstehen damit durch das
    // loosebounds=1 keine Lücken am Kartenrand entstehen
    x = -20;
    y = -20;
    hgt = 40;
    wdt = 140;
    turbulence = 5000;
    loosebounds = 1;
  };
};

