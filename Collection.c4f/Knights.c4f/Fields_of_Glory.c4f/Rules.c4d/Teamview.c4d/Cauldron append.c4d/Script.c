#strict 2
/*
#include ACLD
#appendto ACLD


private func Brewing() 
{
	if(FindObject(RTVW)) SetPlrViewRange(10);   
	if(inherited()) {
		if(FindObject(RTVW)) SetPlrViewRange(0);
		return 1;
	}
	return 0;
} */