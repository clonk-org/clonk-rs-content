packed
/.github/pack-content/target

# Output of the packaging tool, which the validation workflow builds and which
# is easy to leave behind after running the same commands by hand.
/content.zip
/content.sha256
/first.zip
/first.sha256
