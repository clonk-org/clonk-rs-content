# Third-party derivative provenance

This is the policy and patch ledger for imported working trees. `binary` in
`.gitattributes` controls Git checkout and line-ending behavior. It is not a
licence, ownership assertion, provenance record, exact-copy claim, or
localization-audit exemption.

A scope may be called an exact copy only when a named source artifact, its
source hash, the exact path and packing boundary, and the redistribution basis
are recorded. Exploding, selecting, deduplicating, nesting, renaming,
translating, fixing, or repacking makes the result a maintained derivative or
curated import. Do not label a parent tree exact when it contains changed
children.

Public availability, an author field, a source URL, a checksum, a historical
commit, or prior project redistribution does not by itself grant permission to
redistribute or modify a work. Unknown terms remain unknown. This ledger
records evidence; it does not create or expand rights.

The local maintenance decisions are recorded separately in
[RIGHTS.md](RIGHTS.md) and `packs.toml`. Assumed permission does not replace
the evidence classifications below.

## Source and rights ledger

`unknown` means the repository does not retain enough evidence to make the
claim. Git tree IDs identify imported project bytes, not external source
archives.

| Scope | Classification | Source artifact and source hash | Authors and notices | Recorded basis and rights status | Imported project baseline | Localization |
|---|---|---|---|---|---|---|
| `ClonkMars.c4d`, `ClonkMars.c4f` | maintained derivative | external archive and SHA-256: `unknown` | embedded `Author.txt`, `Credits.txt`, and `Copyright.txt` | repository history records project-specific exact-copy redistribution permission; the record was deleted by [#41](https://github.com/clonk-org/clonk-rs-content/pull/41); derivative permission: `unknown` | commit [`c90bafba`](https://github.com/clonk-org/clonk-rs-content/commit/c90bafbaeafa22ed0fb2269613f29de6432b5786); trees `ef9a9256f7155c21a1c712aa6d1b5b78080385c1`, `851de1ffa5e7d708dd7521bc7752a39a75e82338` | [#78](https://github.com/clonk-org/clonk-rs-content/issues/78) |
| `EkeReloaded.c4d`, `EkeReloaded.c4f` | maintained derivative | external archive and SHA-256: `unknown` | embedded `Author.txt` and pack credits | repository history records project-specific exact-copy redistribution permission; the record was deleted by [#41](https://github.com/clonk-org/clonk-rs-content/pull/41); derivative permission: `unknown` | commit [`c90bafba`](https://github.com/clonk-org/clonk-rs-content/commit/c90bafbaeafa22ed0fb2269613f29de6432b5786); trees `228dba3135e5d715a7a7e4f5668b29f85179de16`, `efdf7a71cc7a9eac7cb9ddd9ba419c8eb3dce953` | [#74](https://github.com/clonk-org/clonk-rs-content/issues/74) |
| `Melees.c4f/Queron3.c4s` | maintained derivative | CCAN entry 5737; artifact name and SHA-256: `unknown` | `Author.txt` and the script's author/version line | historical source record describes a public reuse note conditioned on preserving authorship; exact permission scope: `unknown` | commit [`d34d3855`](https://github.com/clonk-org/clonk-rs-content/commit/d34d385591134ce6c262b8c9ed53faaa6229cc6b); tree `b6c1018e143005c50a6456be45c35354f7fbd233`; baseline already localized | parent [#68](https://github.com/clonk-org/clonk-rs-content/issues/68), child [#72](https://github.com/clonk-org/clonk-rs-content/issues/72) |
| `MetalMagic.c4d`, `MetalMagicExtra.c4d` | maintained derivative | CCAN entry 4068, `MM3_1c.zip`; SHA-256: `unknown` | embedded author and description files | the CCAN entry recorded no redistribution terms; [#5](https://github.com/clonk-org/clonk-rs-content/issues/5) closed not planned when the written requirement was deleted, not when rights were resolved | commit [`d34d3855`](https://github.com/clonk-org/clonk-rs-content/commit/d34d385591134ce6c262b8c9ed53faaa6229cc6b); trees `7420fa6f84109863781943f2cd1355952f026a55`, `756a3d628ec3fe948347d1da65d423b4a83f2016`; baseline already localized | [#72](https://github.com/clonk-org/clonk-rs-content/issues/72) |
| `Collection.c4f`, Golems, Modern Combat, and retained compilation dependencies | curated import | `UltimateClonkCompilation-v3.0.zip`; SHA-256 `f6fce16b6bb4c9e150ba239876568a84187ab0c3e2ad45619d11fdb5e08214c3`; full source record in [`UltimateClonkCompilation/PROVENANCE.md`](UltimateClonkCompilation/PROVENANCE.md) | compilation `Authors.txt` plus notices inside individual works | compilation readme leaves works with their individual authors; blanket redistribution and derivative terms: `unknown` | commit [`3ecc43c7`](https://github.com/clonk-org/clonk-rs-content/commit/3ecc43c791eb9d7c1c4bb8f1c038ea2fd6a95f70); Collection tree `e11d9419be40c227be237a2655cf52bc0fe69da4` | [#68](https://github.com/clonk-org/clonk-rs-content/issues/68)-[#77](https://github.com/clonk-org/clonk-rs-content/issues/77) |
| `Collection.c4f/Fun.c4f/ClonkPartyRemake8-clonk-rs-localized.c4s` | maintained derivative | CCAN entry 6111, `Clonk_Party_Remake.zip`; archive SHA-256 `1cebf848fee9611d4fa90c6f7c5b78a8c6a5d07701ea3735137d12c65d882586`; original packed scenario SHA-256 `103577bbe25fa3773923eff62dd0de1a37bc00a0e29561e3fc168c1687a425a0` | CCAN description and scenario resources retain the original remake credits; packaged `ClonkRsLocalization.txt` records the derivative and source | the CCAN entry says the ground-up remake may be freely copied or modified and asks changed versions to provide a version history and a new filename; both are included in this derivative | commit [`b9214caf`](https://github.com/clonk-org/clonk-rs-content/commit/b9214cafb46ac1fd82293d1fe9f2f78a48831a47); original packed scenario is byte-identical to CCAN entry 6111 | [#58](https://github.com/clonk-org/clonk-rs-content/issues/58) |
| `E.P.I.C.c4f` | curated import | maintainer archive; packed `E.P.I.C.c4d` SHA-256 `3ed243a408bd204b94b7a96a0f556ddc5c595e874d3236dc42fb33013055a488`; original scenario-container artifact and SHA-256: `unknown`; details in [`EPIC/PROVENANCE.md`](EPIC/PROVENANCE.md) | embedded credits name Zagabar, Wertilq, Elwebbaro, and other contributors | redistribution and derivative terms: `unknown` | commit [`b9214caf`](https://github.com/clonk-org/clonk-rs-content/commit/b9214cafb46ac1fd82293d1fe9f2f78a48831a47); exploded tree `266844cedfa6afb4eb7de03332d640daedb82c36` | [#78](https://github.com/clonk-org/clonk-rs-content/issues/78) |

## Project patch ledger

| Commit | Scope | Source baseline | Project-authored change |
|---|---|---|---|
| [`2cb2a7bd`](https://github.com/clonk-org/clonk-rs-content/commit/2cb2a7bdd4053f093c0192f3dddbaa0acd850b41) | ClonkMars, Eke Reloaded | `c90bafba` imported trees above | Added and repaired English player-visible text and metadata. |
| [`d34d3855`](https://github.com/clonk-org/clonk-rs-content/commit/d34d385591134ce6c262b8c9ed53faaa6229cc6b) | Queron 3, Metal & Magic | external unmodified archive hashes `unknown` | Imported working trees with project-added English localization. |
| [`963e8cb4`](https://github.com/clonk-org/clonk-rs-content/commit/963e8cb4458b220fc4b8ca18be7ae6ced75f2f5a) | MetalMagicExtra | `d34d3855` tree | Replaced Troll crew sheets with renders from the original rigs. |
| [`3ffa183d`](https://github.com/clonk-org/clonk-rs-content/commit/3ffa183d4d70e1cf5c69996ff110a5971a2f10bf) | Eke Reloaded | `c90bafba` tree plus `2cb2a7bd` | Changed HarpoonRace relaunch-message behavior. |
| [`201c4a24`](https://github.com/clonk-org/clonk-rs-content/commit/201c4a24edf17d322ffcd16ef7c79491189aa95a), reverted by [`e82d6d27`](https://github.com/clonk-org/clonk-rs-content/commit/e82d6d275741dca4d520de6f51c21e87767d6b78) | ClonkMars, Eke Reloaded, Metal & Magic | then-current working trees | Super-resolved preview art, then reverted it; no net effect remains. |
| [`2772c602`](https://github.com/clonk-org/clonk-rs-content/commit/2772c602a0142fcf50879a44bb64d6005ecbc7ab) | ClonkMars | then-current working tree | Removed a stale logo override. |
| [`3ecc43c7`](https://github.com/clonk-org/clonk-rs-content/commit/3ecc43c791eb9d7c1c4bb8f1c038ea2fd6a95f70) | Ultimate Clonk Compilation | v3.0 archive above | Selected dependencies and exploded scenario containers for repository discovery. |
| [`b9214caf`](https://github.com/clonk-org/clonk-rs-content/commit/b9214cafb46ac1fd82293d1fe9f2f78a48831a47) | Collection, E.P.I.C. | compilation archive and maintainer E.P.I.C. snapshot above | Moved private definitions, removed duplicates and unused packs, and added exploded E.P.I.C. scenarios. |
| [clonk-org/clonk-rs-content#58](https://github.com/clonk-org/clonk-rs-content/issues/58) | Collection Fun Clonk Party Remake | CCAN entry 6111 artifact and original packed scenario recorded above | Added reviewed English runtime localization, renamed the derivative, and packaged its source and version history while retaining credits, substitutions, encoding, and nested pack boundaries. |
| [clonk-org/clonk-rs-content#90](https://github.com/clonk-org/clonk-rs-content/issues/90), [clonk-org/clonk-rs-content#82](https://github.com/clonk-org/clonk-rs-content/issues/82) | Collection Settling `RufDerWipfeRE.c4f/Kampagne.c4f/4Schweids.c4s/Ueberlandleitung.c4d/Script.c`; Collection BaseMelees `ClassicMelees.c4f/Objects.c4d/Teamview.c4d/Clonk append.c4d/Script.c` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Two authored script defects that also fail under the pinned C++ engine. `UBRL::IsFulfilled` declared `var pObj` inside an `if` condition, which the C4Script parser rejects, so the power-line goal never evaluated; the declaration now precedes the condition. The Teamview `#appendto CLNK` called `FindObject(RTVW)->ScheduleCall(...)` unguarded on `Departure`, failing with a zero call target in scenarios that load the pack without the `RTVW` rule (ColdSkies); it now guards the lookup the way the pack's Fire and Foundry appends already do. CRLF and ASCII preserved, no other byte changed. |
| [clonk-org/clonk-rs-content#84](https://github.com/clonk-org/clonk-rs-content/issues/84) | Collection Hazard `QuakeR.c4f`: `Script.c` of all eleven scenarios (`Q4_Map 1` to `Q4_Map 4 Chinatown`, `Q_CtF-OpenSky`, `Q_DM-BloodySun`, `Q_DM-DeepDown`, `Q_DM-Fray`, `Q_DM-FrostBite`, `Q_DM-Medieval`, `Q_DM-Renegade`) | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | An authored type mismatch that also fails under the pinned C++ engine. Every scenario declared `RelaunchPlayer(int iPlr, object pCrew, object pKiller, int iTeam)`, while the pack's own callers pass a number as the third argument (`QBot::Death` passes `GetKiller(this())`, the Manager rule passes `-1`), so the typed call was refused and a player whose last bot died was never relaunched. No scenario reads the argument, so the third parameter is now untyped; the shared `QuakeR.c4d` callers are unchanged. CRLF and ASCII preserved, seven bytes removed per file, no other byte changed. |
| [clonk-org/clonk-rs-content#95](https://github.com/clonk-org/clonk-rs-content/issues/95), [clonk-org/clonk-rs-content#96](https://github.com/clonk-org/clonk-rs-content/issues/96) | `Western.c4d/Items.c4d/Tools.c4d/Sack.c4d/Script.c`; `Western.c4d/Structures.c4d/Camp.c4d/MainTipi.c4d/Script.c` | official LegacyClonk `Western.c4d`, CC BY-NC 4.0 (`Western.c4d/COPYING`), as tracked in this repository | Two context rows were offered in states where their callbacks can only refuse, silently. The sack's `ControlDigDouble` row gains `Condition=CanBeCollectedBy`, which mirrors the callback's `FindContents(SAC1,pClonk)` guard, and the main tipi's `ContextJoinClan` row gains `Condition=CanJoinClan`, which mirrors `ContainedDig`'s owner, hostility and clan guards. Both callbacks keep their guards. CRLF and ISO-8859-1 preserved, no other byte changed. |
| [clonk-org/clonk-rs-content#89](https://github.com/clonk-org/clonk-rs-content/issues/89), [clonk-org/clonk-rs-content#92](https://github.com/clonk-org/clonk-rs-content/issues/92) | `Collection.c4f/Settling.c4f/RufDerWipfeRE.c4f/Kampagne.c4f/2Teudoburger.c4s/Script.c`; `Helper.c4d/BuildAccelerator.c4d/Script.c` inside the packed `Collection.c4f/Settling.c4f/m0XeronSettlement.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Two authored script defects. `2Teudoburger` greeted each player with `Golden(iPlayer)`, a function that exists nowhere in the bundle; the pack's golden age is the `IntGoldAge` effect started by `StartGoldenAge(owner)` in `RufDerWipfe.c4d/System.c4g/Golden.c`, and `Golden` is what is left of an object-based version the pack's music script still looks for (`_GLZ`, which no longer ships), so the mission now calls `StartGoldenAge(iPlayer)`. That reading is an inference from the pack, not a recorded author statement. m0Xeron's `BACC` rule dereferenced `GetActionTarget()` on every Clonk in `Build`, while the scenario's own `InitializePlayer` poses the arriving skeleton in a targetless `Build`; it now resolves the target once and skips a builder that has none. The packed scenario was edited on its raw group image: the entry's bytes, its size and CRC, and the size and CRC of the two enclosing child entries. No other record, entry, time stamp or header byte changed, and the pinned C++ `c4group` explodes the result to the same 230 files with that one script differing. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#85](https://github.com/clonk-org/clonk-rs-content/issues/85) | `DCQ3.c4d/Tower.c4d/ScriptDE.c`, now `Script.c`, inside the packed `Collection.c4f/Puzzles.c4f/Das_Clonk_Quiz_3_Meister_des_Quiz.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | The scenario overloads the castle tower `CPT2` with a definition whose only script was `ScriptDE.c`. A definition's script is `Script.c`, then `Script<LANG>.c` for each language of the player's sequence, and neither shipped language file names a fallback, so under a US player the tower had no script and the tower parts that include it failed on its helpers (`FindDrawbridgeUp`, `FindStaircaseAbove`), in the pinned C++ engine as well. The script holds no player-visible string, only function, action and sound names, so it is language-neutral and the entry is renamed to `Script.c`; its bytes are unchanged and a German player loads the same script as before. Edited with `tools/group_entry.py`: the one name is the only difference in the group image (these records carry no stored CRC), and the pinned C++ `c4group` explodes both versions to the same 129 files apart from that name. |
| [clonk-org/clonk-rs-content#87](https://github.com/clonk-org/clonk-rs-content/issues/87) | `Objects.txt` inside the packed `Collection.c4f/Settling.c4f/DieNeueWelt.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | An authored save defect that also fails under the pinned C++ engine. The scenario restores Storeframework `STFW` `#55662` holding three pieces of smoked meat, but with `LocalNamed=0`. The rack makes its `iMeat` and `iTime` arrays in `Initialize`, which a restored object never runs, so every `CheckMeat` timer call indexed nil. The record now carries the three locals as the engine saves them for a rack (`Western.c4f/Goldrush.c4s/Objects.txt` has the same shape): `iMeat` holds the three contained objects in its first slots, `iTime` is empty and `iCollectObj` is nil. The rack's item overlays are not restored; they are presentation only, and the save does not say what they were. Edited with `tools/group_entry.py`: the entry's bytes, size and CRC are the only difference in the group image, and the pinned C++ `c4group` explodes both versions to the same 494 files with that one entry differing. CRLF and ASCII preserved. |
| [clonk-org/clonk-rs-content#80](https://github.com/clonk-org/clonk-rs-content/issues/80) | `Scenario.txt` of the twelve playable `Collection.c4f/Adventures.c4f/MissionsHarkon.c4f/Mission1A.c4s` to `Mission2G.c4s`, and of the packed `Collection.c4f/Adventures.c4f/Adventure.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | An import layout defect that also fails under the pinned C++ engine. These scenarios name `MetalMagic.c4f\Misc.c4d`, while this repository keeps that folder only as `Collection.c4f/Knights.c4f/MetalMagic.c4f`. `C4GameResList::Load` opens each definition name against the data root and fails the start with `IDS_PRC_DEFNOTFOUND` when it is not there, so none of the thirteen could start. They now name `Collection.c4f\Knights.c4f\MetalMagic.c4f\Misc.c4d`. The `MaxPlayer=0` chapter entries name no such definition and are unchanged, as is Adventure's `Origin`. Adventure was edited with `tools/group_entry.py`, and the pinned C++ `c4group` explodes both versions to the same 980 files with only `Scenario.txt` differing. CRLF and ASCII preserved. |
| [clonk-org/clonk-rs-content#67](https://github.com/clonk-org/clonk-rs-content/issues/67) | `Collection.c4f/Settling.c4f/RufDerWipfeRE.c4f/Tutorial.c4s/Script.c` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | An authored script defect that also fails under the pinned C++ engine. The tutorial declared `Script410` twice. C4Aul binds the one declared last, so the hut the player is told to build was never checked, and the crew check's wait, `goto(418)`, ran `Script420` again and let the script counter run on to `Script990`, which fulfils the goal unaided. The crew check is now `Script430` with `goto(428)`, waiting on itself as every other check in the file does. LF line endings and ISO-8859-1 preserved, two tokens changed. |
| [clonk-org/clonk-rs-content#59](https://github.com/clonk-org/clonk-rs-content/issues/59) | `Collection.c4f/Adventures.c4f/Faffnir.c4f/faffnir_1.c4s/Script.c`, `Faffnir_2.c4s/Script.c`; `Collection.c4f/Adventures.c4f/DerGoldeneWipfMutli.c4f/Der goldene Wipf2multi.c4s/Script.c` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Two authored script defects, the same under the pinned C++ engine. Both Faffnir missions hand the assassin a knife pack, `KNFP`, and cleared the previous kit on each relaunch with `RemoveAll(KNPF)`. `KNPF` names no definition, which C4Aul treats as a plain id constant, so every other item was cleared and the knife packs piled up; the cleanup names `KNFP` now. Der goldene Wipf 2's `RelaunchPlayer` healed `obj`, a name it never declares, so under `#strict` every relaunch ended in an error after making the replacement Wipf; it heals `clnk`, the Wipf it just made. CRLF and ISO-8859-1 preserved, one token per file. |
| [clonk-org/clonk-rs-content#60](https://github.com/clonk-org/clonk-rs-content/issues/60) | `Script.c` inside the packed `Collection.c4f/Fun.c4f/WarStars1_1.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Two authored defects in one function, the same under the pinned C++ engine. The pack's Clonk relaunches through `GameCallEx("RelaunchPlayer", ..)` from `Destruction`, but the scenario declared `RelaunchPlayer` as a `global func`, which C4Aul gives the engine rather than the scenario script, so the scenario-script call never reached it. It also stored each player's home ship in `Global(player + 1000)` and read `Global(player)`, which is never set, and returned there. It is a scenario `func` now and reads `Global(player + 1000)`. Edited with `tools/group_entry.py`; the pinned C++ `c4group` explodes both versions to the same 175 files with only `Script.c` differing. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#61](https://github.com/clonk-org/clonk-rs-content/issues/61) | `Script.c` inside the packed `Collection.c4f/Knights.c4f/(G)IceCorpse.c4s` and `Arenafight137.c4s`; `Collection.c4f/Knights.c4f/Rebellion.c4f/Bridge.c4s/Script.c` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Three authored script defects, the same under the pinned C++ engine. IceCorpse's `Script1` closed itself with a stray `}` halfway through filling the coach, so its bows, arrows, coke and flints were statements outside any function and never ran; the brace is removed. Arenafight137's `Sieger` loop was `while(i > 10 && i != iMax)` from `i = 0`, which never runs, so no player was eliminated before `GameOver`; it is `while(i < 10)` with `if(i != iMax)` around `EliminatePlayer`. Bridge counted the right side's extra players with `GetTeamPlayerCount(Position_Left)`, so a right side of more than four left players without a knight; it counts `Position_Right`. The packed groups were edited with `tools/group_entry.py`, and the group images differ only in `Script.c`. The pinned C++ `c4group` explodes IceCorpse to the same 396 files with only `Script.c` differing; it cannot fully explode Arenafight137 in either version. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#63](https://github.com/clonk-org/clonk-rs-content/issues/63) | `Script.c` inside the packed `Collection.c4f/Melees.c4f/DepthCharge.c4s`, `JungleFlurry.c4s`, `MoonmapRejoin.c4s`, `TemplePushing.c4s` and `Z4BattleOfGenerations.c4s`; `Scenario.txt` inside the packed `Icechasm.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Six authored defects, the same under the pinned C++ engine. DepthCharge scored a death by nature to `Global(-1)`, which is slot 0, the first player's score; a death by nature is now only reported. JungleFlurry's `MakeLight` read `GetPlayerByIndex(plr)` with an unset `plr` instead of the loop's `cnt`. MoonmapRejoin's spawn picker bounded its index by `GetLength(id)` instead of `GetLength(id) - 1`, so late spawns could pick nil. TemplePushing's `MaxTeamCount` was 3 against four teams, so the fourth team's scoreboard row was the first player's; it is 4. Z4BattleOfGenerations announced every death but never counted it, so no team ever reached its ten kills; `OnClonkDeath` now keeps the tallies the scenario description states, a point per enemy killed and one off for a team kill or suicide. Icechasm's loader named `FarWorlds.c4f/Arctic.c4f`, while the pictures are in `FarWorlds.c4f/Arctic.c4s`. Edited with `tools/group_entry.py`; each group image differs only in the one entry, and the pinned C++ `c4group` explodes every pair to the same files with only that entry differing. Line endings and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#65](https://github.com/clonk-org/clonk-rs-content/issues/65) | inside the packed `Collection.c4f/Puzzles.c4f/`: `Das_Clonk_Quiz_3_Meister_des_Quiz.c4s/Script.c`, `TempleEscape_League.c4s/Script.c`, `1_S2Curious.c4s/{Objects.txt,Script.c,System.c4g/Holy.c}`, `2_MountVikto.c4s/Objects.txt`, `4_TowerOfMagic.c4s/Objects.txt` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Authored defects, the same under the pinned C++ engine. The Quiz declared the label `Script340` twice and C4Aul binds the later one, so the first, between `Script327` and `Script335`, is `Script330` now. TempleEscape's `MakeLight` read `GetPlayerByIndex(plr)` with an unset `plr` instead of the loop's `cnt`, and two crew loops advanced `cnt` instead of `cnt2`. Castle Curious, Mount Vikto and Tower of Magic were saved against an older Metal & Magic and placed its sainthood, the sainthood's helper and the Cra-Kla-Zoth staff as `HLTM`, `HTMH` and `SM8W`; Metal & Magic 3.1b ships them as `SNHD`, `SNHP` and `SCKZ`, with the same category and shape, and an object whose id names no definition is skipped on load. The object records, the effects' definition fields, Castle Curious's `#appendto` and its wall text use the current ids now. `HLET` has no confirmed counterpart and is unchanged. Edited with `tools/group_entry.py`; each group image differs only in the named entries, and the pinned C++ `c4group` explodes every pair to the same files with only those entries differing. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#66](https://github.com/clonk-org/clonk-rs-content/issues/66) | inside the packed `Collection.c4f/Races.c4f/`: `tschudispecial.c4s/Script.c`, `RopeRace.c4s/Script.c`, `AbwaertsExtremTeam2.c4s/{Script.c,System.c4g/Redefine.c}`, `AbwaertsFalschrum.c4s/{Script.c,System.c4g/Redefine.c}` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Authored defects, the same under the pinned C++ engine. tschudispecial declared `InitializePlayer` twice and C4Aul binds the later one, so the first, which started the scenario script with `ScriptGo(1)`, never ran; the remaining `InitializePlayer` calls it. RopeRace set its "Again" mission to `RopepackRemake.c4f\RopeRace.c4s`, its path in its own pack, and names `Collection.c4f\Races.c4f\RopeRace.c4s`, where the import keeps it, now. Both Abwaerts races redefine each Clonk as an Aquaclonk for the contact calls their fall damage needs, then drew it from the Clonk's sheet, or from the scenario's own `_INE` sheet for a player whose name ends in "ine". The Aquaclonk walks in cells 20 wide, where the Clonk sheet's are 16 wide and `_INE`'s whole sheet is in 16 by 20 cells, so walking Clonks slid out of their cells (clonk-org/clonk-rs#1471). Both `SetGraphics` calls are gone and the Clonks are drawn from the Aquaclonk's own sheet; the `_INE` portrait stays, and the Gunpowder's `SetGraphics(0, pClonk, MCLK)` is unchanged. Edited with `tools/group_entry.py`; each group image differs only in the named entries, and the pinned C++ `c4group` explodes every pair to the same files with only those entries differing. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#64](https://github.com/clonk-org/clonk-rs-content/issues/64) | `Script.c` inside the packed `Collection.c4f/ModernCombat.c4f/CMC_Train.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Authored defects, the same under the pinned C++ engine. `Initialize` called `CreateEquipment()` twice, so every ammunition crate and the weapon vending machine stood twice on the same spot; the second call and its comment are gone. `CreateDecoration` placed three of its four animated screens with a comma missing before the owner, as in `CreateObject (SCA1, 698, 435 -1)`, which passes `y - 1` and leaves the owner 0, so the first player owned them; they are placed at their `y` without an owner now, as the fourth is. Edited with `tools/group_entry.py`; the group image differs only in that entry, and the pinned C++ `c4group` explodes both to the same files with only that entry differing. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#121](https://github.com/clonk-org/clonk-rs-content/issues/121) | `Rope.c4d/Script.c` inside the packed `Collection.c4f/Races.c4f/Ropepack.c4d`; `Value.c4d/Script.c` of `Collection.c4f/BaseMelees.c4f/ClassicMelees.c4f/GrantSettlingFeast.c4s` and `LittleSettlingFeast.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Authored defects, the same under the pinned C++ engine. The rope (`171E`) ended with `LineBr eak: return(0);`, which C4Aul stops parsing at `eak`, so it never overrode the `LineBreak` it includes from `1E1E`; it reads `LineBreak:` now. The settlement goals called `FindObjectOwner(ACNT, player)->GetAlliancePlayerCount(1)` unchecked in `IsFulfilledforPlr`, `Activate` and `GetTargetScore`, which fails for a player still choosing a team; each checks for the account first now, as the goal's own `AllPlayersHaveValue` does. The rope pack was edited with `tools/group_entry.py` and differs only in that entry, and the pinned C++ `c4group` explodes both images to the same files with only that entry differing. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#81](https://github.com/clonk-org/clonk-rs-content/issues/81) | `Collection.c4f/Adventures.c4f/InExantros.c4f/2.Akt.c4s/Script.c` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | An authored defect, the same under the pinned C++ engine. `func Initialize() {` at line 43 was never closed, so C4Aul ended it at the bare `RelaunchPlayer:` label, which then declared nothing, and a hero who died was never relaunched. A `}` after `Initialize`'s `return(1);` closes it, as in the first act. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#123](https://github.com/clonk-org/clonk-rs-content/issues/123) | `Scenario.txt` `[Definitions]` of the 45 `Collection.c4f` scenarios listed in the issue, 11 of them inside packed groups, and of five `E.P.I.C.c4f` scenarios: `Death_Canyon.c4s`, `Hills_of_gore.c4s`, `Minor_Wound_Battle.c4s`, `Test map.c4s` and `Wound_Valley_King.c4s` | `UltimateClonkCompilation-v3.0.zip` tree and maintainer E.P.I.C. snapshot recorded above, as imported in `b9214caf` | Layout defect: each name is the module's path in its original pack, but here the module sits in one of the scenario's enclosing folders rather than at the data root. `C4GameResList::Load` opens a definition name against the working directory only (C4GameParameters.cpp:199-207, C4Group.cpp:660-677), so none of these scenarios started under the pinned C++ engine. Each name now spells the module's path from the data root, which is the module clonk-rs already found through its enclosing-folder fallback, and `tests/test_scenario_definitions.py` checks every scenario. The packed scenarios were edited with `tools/group_entry.py`, which now also rewrites the data-only CRC of a `C4GECS_Old` record (the one in `Mirror_of_Dreams.c4s`). Each group image differs only in `Scenario.txt`, and the pinned C++ `c4group` explodes every pair to the same files with only that entry differing. For `ClonkPartyRemake8-clonk-rs-localized.c4s` it stops in both images at a child section group whose ISO-8859-1 name macOS cannot create. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#124](https://github.com/clonk-org/clonk-rs-content/issues/124) | `Scenario.txt` `[Definitions]` of ten `Collection.c4f/Knights.c4f/MetalMagic.c4f` scenarios: `Assassination.c4s`, `BattleForWedok.c4s`, `CastleConquest.c4s`, `GriffonRace.c4s`, `Kamteiusvalley.c4s`, `SacredHills.c4s`, `WarOfCKZ.c4s` and `Tutorials.c4f/{Tutorial,Tutorial2,Tutorial3}.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | Content defect, the same under the pinned C++ engine. Each scenario saved objects whose ids only `MetalMagicExtra.c4d` defines, such as the No Undead and Need Spellstones rules, Cultsites, Life amulets, the Priest, the Mystic and `Tutorial3`'s Kanderian palace, but did not load that module. An object whose definition is not loaded is skipped, so none of them existed. `MetalMagicExtra.c4d` now follows `MetalMagic.c4d`, as in the Harkon missions, and the other modules keep their order. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#59](https://github.com/clonk-org/clonk-rs-content/issues/59) | `Collection.c4f/Adventures.c4f/InExantros.c4f/1.Akt.c4s/Script.c` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | An authored defect, the same under the pinned C++ engine, repaired by inference. `Initialize` creates the fourth cave elevator, `ELEV`, at (5587,9728), far below the 1450-pixel landscape (`Map.bmp` is 602x145 at zoom 10), so it cannot be reached. The author's value is lost; 928 replaces 9728. Removing one digit gives 928, 972 or 978, and all three land in the open cave that spans y=830-1130 at x=5587. The act's Manaquelle stands in that cave at (5440,920). The other three elevators hang 37 to 99 pixels under the rock above them; 928 hangs 98 pixels under it, and 972 and 978 would hang 142 and 148. CRLF and ISO-8859-1 preserved. |
| [clonk-org/clonk-rs-content#69](https://github.com/clonk-org/clonk-rs-content/issues/69), first batch | Collection Adventures: the packed `Magiertest.c4s`, `Stippel_vs_SFT_Reloaded.c4s`, `RokanComingBack.c4s`, `Adventure.c4s` and `Cloudtemple3.3.c4s`, and the exploded `Faffnir.c4f` and `DerGoldeneWipfMutli.c4f` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the exact-copy decision recorded on [clonk-org/clonk-rs-content#68](https://github.com/clonk-org/clonk-rs-content/issues/68) on 2026-09-23. German player-visible script literals moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them, and missing or poor English selector descriptions, titles, object names, descriptions and string-table values were translated or rewritten from the German. One authored defect: `Adventure.c4s` defined `QstFukatFreeDesc2` twice, and `C4LangStringTable::ReplaceStrings` takes the first entry, so the `FinishQuest` that frees Fukat announced his arrest; the release now has its own `QstFukatFreeDesc3`. Credits, ISO-8859-1, CRLF and `%`/`$Key$` substitutions are preserved, and packed groups were edited in place with `tools/group_entry.py`. |
| [clonk-org/clonk-rs-content#69](https://github.com/clonk-org/clonk-rs-content/issues/69), second batch | Collection Adventures: the exploded `Feindesland.c4f` and its four scenarios, `DerMagischeWald.c4s`, `Kasernensturm.c4s`, `KerkerFlucht.c4s` and `Magiergilde.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the same exact-copy decision as the first batch. The options menus, difficulty and relaunch messages, the three books' lines and the torches' message moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them. The `Spione` team name moved into each scenario's root tables; `C4Game.cpp:235` hands that table to `C4TeamList::Load`, which localizes `Teams.txt` (`C4Teams.cpp:650-651`). Missing selector descriptions, titles, object names and descriptions were translated from the German, and five broken or truncated English descriptions and one misspelled object name were rewritten. `Kasernensturm.c4s/Tower.c4d` defined `TxtLeftgate` and `TxtRightgate` twice in both tables; `C4LangStringTable::ReplaceStrings` takes the first entry, so the unused second pair was removed and the shown text is unchanged. Credits, ISO-8859-1, CRLF and `%`/`$Key$` substitutions are preserved. |
| [clonk-org/clonk-rs-content#69](https://github.com/clonk-org/clonk-rs-content/issues/69), third batch | Collection Adventures: the packed `TheTempleRemaster.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the same exact-copy decision as the first batch. The scenario's English dialogue was reviewed against the German and rewritten where it was misspelled or changed the meaning; the translator's voice is kept. The chapter log lines, the ghost's, magic door's and switch's messages, the item pickup messages, and the clonk scripts' ammunition messages and German context-menu captions moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them, and `System.c4g` got the English table it lacked. The missing selector description and object descriptions were translated from the German. Definitions named only by a German DefCore `Name`, and group names with only a `DE:` line, got an English name beside the unchanged German (`C4Def.cpp:637-638` takes the language's line and otherwise keeps DefCore's `Name`). Credits, ISO-8859-1, CRLF and `%`/`$Key$` substitutions are preserved, and the group was edited in place with `tools/group_entry.py`. |
| [clonk-org/clonk-rs-content#69](https://github.com/clonk-org/clonk-rs-content/issues/69), fourth batch | Collection Adventures: `InExantros.c4f/InExantros.C4D`, the adventure's packed object pack | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the same exact-copy decision as the first batch. The hero's menus, stats, help texts and messages, the merchant's, deity's, difficulty menu's and battle cries' lines, and the item, weapon, spell and building messages moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them. A caption spelled like the function its menu item calls was replaced only in the caption. The twelve spell parchments got the English tables they lacked. German left in the hero's and enemy's English tables was translated, and their two missing keys were added with the German table's single-space value. Missing selector and object descriptions were translated from the German. Wrong or copied English descriptions and names were rewritten from the German, for example the scrolls, armor, helmets, boots and weapons. Definitions named only by a German DefCore `Name` got a `Names.txt` with the same German. `Living.c4d/Kröte.c4d/DescDe.txt` was renamed to the canonical `DescDE.txt`. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved, and the pack was edited in place with `tools/group_entry.py`. Its child groups whose records carry a legacy CRC keep that value, which neither engine reads. |
| [clonk-org/clonk-rs-content#69](https://github.com/clonk-org/clonk-rs-content/issues/69), fifth batch | Collection Adventures: `InExantros.c4f/INEXSzenarioObjekte.c4d`, the adventure's packed NPC and scenario-object pack | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the same exact-copy decision as the first batch. The NPCs' menu dialogue, replies and diary entries, and the messages of the quest objects, idols, gravestones, dwarf book, monastery puzzle, bosses and waypoints moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them. The dialect speakers keep a matching English voice; incantations, Latin, passwords and the German voice files stay as they are. Missing object descriptions were translated from the German. Names still German or misspelled in English were translated, and visible definitions named only by a German DefCore `Name` (the NPCs, quests, items and buildings) got a `Names.txt` with the same German. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved, and the pack was edited in place with `tools/group_entry.py`; its legacy-CRC child groups keep their stored value. |
| [clonk-org/clonk-rs-content#69](https://github.com/clonk-org/clonk-rs-content/issues/69), sixth batch | Collection Adventures: the three `InExantros.c4f` acts (`1.Akt.c4s`, `2.Akt.c4s`, and `3.Akt.c4s` with its final-boss definitions and credits waypoints in `Daemonenwelt.c4d`), and the names the first batch left in `Cloudtemple3.3.c4s` and `DerGoldeneWipfMutli.c4f` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the same exact-copy decision as the first batch. The acts' signs, gravestones, diary chapters, death and relaunch messages, the final boss's and Punzel's lines, and the credits moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them; the credits keep every beta tester's name. The act titles got English names. The first act's English description, which invented a sentence the German does not have, was translated again from the German. The final boss and Punzel's clonk got English descriptions translated from the German. Definitions named only by a German DefCore `Name` got a `Names.txt` with the same German. Credits, ISO-8859-1, CRLF and `%`/`$Key$` substitutions are preserved. |
| [clonk-org/clonk-rs-content#70](https://github.com/clonk-org/clonk-rs-content/issues/70) | Collection Adventures: `MissionsHarkon.c4f`, the Story of Harkon campaign (two chapter markers, twelve missions and the shared `Obj.c4d`) | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the exact-copy decision recorded on #68 for #69–#78. Each mission's goal and sign texts, its story dialogue and dialogue buttons, the guards' barks and talk, the ghosts' verses (rhymed in English too), the card table, the respawn and travel menus, Merlin's and Os's dialogue, and the context-menu captions of function descriptions moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them; the German voice files stay as they are. The fifteen selector descriptions were translated from the German, the selector titles' misspelled or wrong English was corrected, and the missing object descriptions, string tables and keys were added. Object names and descriptions whose English was wrong, misspelled or a placeholder (`A new object definition.`) were translated again from the German. Five `Names.txt` saved as UTF-8, and two missions' goal texts, which C++ shows as mojibake under the shipped charset, are ISO-8859-1 like the rest of the pack. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#71](https://github.com/clonk-org/clonk-rs-content/issues/71) | Collection: `BaseMelees.c4f` (with `ClassicMelees.c4f`) and `Fun.c4f`, except `Fun.c4f/ClonkPartyRemake8.c4s`, which stays with [clonk-org/clonk-rs-content#58](https://github.com/clonk-org/clonk-rs-content/issues/58) | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the exact-copy decision recorded on #68 for #69–#78. Selector descriptions and titles that were missing, a placeholder, German or poor English were written again from the German, and the missing object descriptions, names, string tables and keys were added. English values that were wrong, empty or still German were translated again. The host menus and their toggles, shops and recruiting menus, goal, team, round and kill messages, timers, scoreboard captions, minigame and weapon names, team crests and the other player text written into the scripts moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them. Only captions and announcements got keys where a German literal is also a scenario section's name, so `LoadScenarioSection` keeps its code names. Crazy X-Mas's English foundry and lorry scripts, which still spoke German, were translated in place. Keys that scripts copied from stock definitions use without any table defining them (the BoR sea horse and cannon tower, Zombie Castle's banner and construction kit) were added in both languages with the stock values. The `base_fight` fun-mode messages saved as UTF-8, which C++ shows as mojibake under the shipped charset, are ISO-8859-1 like the rest of the pack. Localization files use their canonical names. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#136](https://github.com/clonk-org/clonk-rs-content/issues/136) | Collection Adventures: function descriptions left German by #69 in `Cloudtemple3.3.c4s`, `InExantros.c4f`, `RokanComingBack.c4s` and `TheTempleRemaster.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the exact-copy decision recorded on #68 for #69–#78. The eight German captions of function descriptions (`[Caption\|...]`), which C++ shows as context-menu entries and command-display labels, moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside their scripts; any `\|Image=` part is unchanged. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#137](https://github.com/clonk-org/clonk-rs-content/issues/137) | UltimateClonkCompilation definition packs outside the scenario-root audit: `WesternBugfixes.c4d`, `WesternBalancing.c4d`, `ModernCombat.c4d` and `RopepackRemake.c4d` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `3ecc43c7` | English localization in place, under the exact-copy decision recorded on #68 for its children. `WesternBugfixes.c4d`'s English tables gained the seven keys its scripts use; the trap and storage values are `Western.c4d`'s own, and two misspelled keys were renamed. Its ten folder descriptions without English were translated. English descriptions and values that were truncated, copied from another object, German or misspelled were translated again from the German. The tent pack's one hard-coded German menu caption moved into its tables. `WesternBalancing.c4d`, `ModernCombat.c4d` and `RopepackRemake.c4d` lost their German words and misspellings in English. `ModernCombat.c4d`'s duplicated keys keep one copy each: the copy whose meaning matches the German, where the two differed. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#141](https://github.com/clonk-org/clonk-rs-content/issues/141) | `WesternBugfixes.c4d`: the tent pack's `DescBackMenu` and `System.c4g`'s `MsgHasZaps` and `WireAlreadyThere` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `3ecc43c7`, plus #137 | English aligned with the maintained `Western.c4d`, whose copied, truncated and misspelled English the same change corrected: "Returns you to the build menu.", "guarding zaps" as the spell is named, and the ignition box rather than a detonator. |
| [clonk-org/clonk-rs-content#72](https://github.com/clonk-org/clonk-rs-content/issues/72) | Collection: `Knights.c4f` (with `MetalMagic.c4f`, `Rebellion.c4f` and `Fields_of_Glory.c4f`); `MetalMagic.c4d`, `MetalMagicExtra.c4d` and `Melees.c4f/Queron3.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf`, plus #124 for Collection; the `d34d3855` trees, plus `963e8cb4`, for Metal & Magic and Queron 3 | English localization in place, under the exact-copy decision recorded on #68 for #69–#78. Selector descriptions and titles that were missing (Arenafight, DynDungeon, the four History chapters and the three Metal & Magic tutorials), German (Avalanche's story, `Schwer` in Crusades) or poor English were written again from the German, as were object descriptions and names that stopped short of the German, said something else or kept German words. Two English titles, a file name (`FeudForTheBorderfortress`) and a misspelling (`Cla-Kra-Zoth`), were corrected. The Metal & Magic tutorials, whose missing English tables showed English players raw keys, got English tables that follow the German line for line. The player text written into the Knights scripts moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them: the tutorials, Dynmelee's game lord, menus and round messages, DynDungeon, Arenafight, Ice Corpse, Fields of Glory's death and relaunch logs, Fish and Wipf, Elevator and Wagon Fight, Zufallsarena, Keepers/Adept and Hellfight. German function descriptions, which label context-menu entries and the command display, became keys too, and a switch caption whose key had lost its `$` delimiters was repaired. The Metal & Magic import's table defects were fixed: keys defined twice keep only the first copy, which `C4LangStringTable` uses, and English that was still German, a German placeholder typo, a missing English key and poor English were corrected. Metal & Magic's Heiligtum, which the English called a "sainthood", is a sanctuary in its English names and text. Four descriptions were stored as UTF-16, which C++ loads as bytes and shows as the empty string before the first NUL; they are now ISO-8859-1 like the rest of the pack. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#144](https://github.com/clonk-org/clonk-rs-content/issues/144) | `Scenario.txt` inside the packed `Collection.c4f/Knights.c4f/(G)IceCorpse.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf`, plus #72 | Content defect, the same under the pinned C++ engine. `Definition1=Objects.c4d ` kept a trailing space, which C++ reads into the module name (`C4Scenario.cpp:491`, `StdCompiler.cpp:998`). `C4GameResList::Load` could not open `"Objects.c4d "` and failed the start with `IDS_PRC_DEFNOTFOUND` (`C4GameParameters.cpp:201-204`), so the scenario never started. The space is removed. Edited with `tools/group_entry.py`, and the pinned C++ `c4group` explodes both versions to the same 403 files with only `Scenario.txt` differing, by that one byte. CRLF and ASCII preserved. |
| [clonk-org/clonk-rs-content#143](https://github.com/clonk-org/clonk-rs-content/issues/143) | Collection: scripts, tables and descriptions in `Adventures.c4f` (Faffnir 2, the Harkon door tower, lock and switch, Rokan's lock and switch, Adventure's and Der goldene Wipf's switches, Feindesland's chest), `BaseMelees.c4f` (BoR, Inselspringen, Wasser, base_fight, EKE vs Western) and `Fun.c4f` (Warofthegods, Ballerburg, SLS2CrownHiding, FlintRainer2); the stock `Western.c4f` temple switches and locks | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf`, plus #69, #70 and #71 for Collection; the maintained `Western.c4f` | What the landed localizations left, under the exact-copy decision recorded on #68. German function descriptions that follow a function's opening brace, which the earlier scans missed, label context-menu entries and the command display; they became keys in paired `StringTblDE.txt`/`StringTblUS.txt` tables beside their scripts, or in `System.c4g`'s own. Seven switch captions written `[DescSwitch]` showed the literal word; they reference the `DescSwitch` their tables already define. The stock `Western.c4f` locks were captioned in English for every player; they get the key and the German the Collection copies now have. Three descriptions were stored as UTF-16, which C++ loads as bytes and shows as C strings, so Ballerburg's shop read "ÿþH" in English and two chests read nothing in German; they are ISO-8859-1 like the rest of the pack. FlintRainer2's copy of the Metal & Magic messages and Warofthegods' goal say "sanctuary" where #72 renamed the "sainthood", and FlintRainer2's Quicksand flint reads as #72 corrected it in the Sty Islands copies. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#73](https://github.com/clonk-org/clonk-rs-content/issues/73) | Collection: `Magus.c4f` and `ModernCombat.c4f`; the top-level `ModernCombat.c4f` (CMC_ResearchFacility, CMC_Skyfall's patrol supply drop and CMC_OperationCaveOffensive's X5 rocket) | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf`, plus #64 | English localization in place, under the exact-copy decision recorded on #68 for #69–#78. Selector descriptions that were missing (Checkerfield, CMC_Fortress), broken, cut short or saying something other than the German were written again from the German. That includes Turnier der Zauberer's note that the game itself was still German, CMC_Train's reversed team sides and its made-up English flag names. The missing object descriptions, names and tables were added, localization files got their canonical names, and a chest description stored as UTF-16, which C++ shows as the empty string before its first NUL, is ISO-8859-1. The player text written into the scripts moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside the scripts that use them: Turnier der Zauberer's story mode, tutorial, spectators and taunts, the KdD magic pack's combo rule and knockback spell, CastCavern's damage tracking, Clonkrix's options, Etagenkampf's spam and spawn protection, bridges2's relaunch and difficulty menus and magic staff, and Z4Balance's league ranks. SkyBridge's menu defined `hStart` and `sRelRandomDesc` twice and misspelled `sSupLessDesc` in both languages, so English and German players saw raw keys; its English was translated again line for line. BlacksCMC-Waffenmod and CMC_Fortress copied ModernCombat.c4d's class, CTF and chooser table without English, and got one with the classes they add. CMC_Train's team list named its teams in German for every player and now uses the table's `Team1`/`Team2`. In the top-level pack, ResearchFacility's English "Turn on"/"Turn off" were swapped, the patrol supply drop's German and English names were swapped, and the X5 rocket's default name was German. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#149](https://github.com/clonk-org/clonk-rs-content/issues/149) | `Script.c`, `StringTblDE.txt` and `StringTblUS.txt` inside the packed `Collection.c4f/Knights.c4f/WagonFight_s2mod.c4s` and `ElevatorFight_Tournament.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf`, plus #72 | Regression from #72, which moved every occurrence of `"Relaunchs"` and `"Doof"` into the string tables. `"Relaunchs"` is also the name of the `local Relaunchs` each scenario's `CLNK.c4d` declares, and `"Doof"` is the command of a menu item that calls `func Doof`. An English game therefore named `Relaunches` and `Dumb` instead. `FnLocalN` returns nil for an unknown name (`C4Script.cpp:4592-4604`), and assigning to that nil stops `RelaunchPlayer` (`C4AulParse.cpp:478`, `C4AulExec.h:310-314`), so the relaunch counter was never written and mixed-language network games desynchronized. Both names are restored to the upstream bytes. The menu caption keeps its translation, and the `Board02` key, never shown to a player, is removed. `ElevatorFight_Tournament` also gets `Elevator04` to `Elevator07` for the four scheduled countdown messages #72 left in German. `C4ScriptHost::MakeScript` replaces placeholders inside those script strings as well (`C4ScriptHost.cpp:66-75`). Edited with `tools/group_entry.py`, with ISO-8859-1 and CRLF preserved. Both scenarios link in the pinned C++ engine with no errors. |
| [clonk-org/clonk-rs-content#74](https://github.com/clonk-org/clonk-rs-content/issues/74) | Collection: `Hazard.c4f` and `Melees.c4f`; `EkeReloaded.c4f`; `EkeReloaded.c4d` (audited, unchanged) | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf`, for Collection; the `c90bafba` tree plus `2cb2a7bd` and `3ffa183d` for Eke Reloaded | English localization in place, under the exact-copy decision recorded on #68 for #69–#78. Selector descriptions that were missing (DM_Airbag, PDM_SteamPower, Melow, SillyVally) were written from the German, and so were those that were garbled, cut short or said something other than the German. That covers Hot Pot's goal and its glow sticks, which defuse mines rather than set them off. It also covers Skyline, Tundra, Crate Fight, four QuakeR maps, Eke Wagon Fight, both KdK scenarios (with KdK's changelog), Moonmap's relaunch, the two Palisade fights, Red Rocket, Rumble in the Jungle and OilWars. Mole's English said four relaunches where the German and its own header say three. SillyVally got a `Title.txt` beside its German `[Head] Title`, the two KdK titles used the `EN:` code, which no language matches, and PalisadeFightIndi's English title lacked the Indians. Missing object descriptions and English that was a placeholder, garbled or German were written from the German. English that is the base game's own translation of the same German stays (Rock, FireArrow, HolyStatue, Bone, Palisade2, Rockfall, Bombe120). Airclaw now uses Metal & Magic's translation of the same text. Three names were wrong or misspelled (Nuklearball as "Granitball", "Pressuire wave", "Materialfall"). Four player-visible definitions with only a German DefCore name got a `Names.txt`. The player text in the scripts moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables: SillyVally's relaunch taunts, gravity message, flint captions and castle-control menu, Melow's settings menu, mirroring and relaunch logs, TIM1 view menu, statue and lorry captions, Moonmap's relaunch logs, Telekinesis, KdK Extreme's greeting and both KdK scoreboards, QuakeR's league prefix and Eke Wagon Fight's menu. Only display positions were touched. Eke Wagon Fight's `LocalN("Relaunchs")` and its `"Doof"` menu command keep their names (#149). IonRifle's English table named `Laser` the key its script asks for as `Strahl`, and the PhysGun asked for a `NotEnoughAmmo` neither of its tables had; that key now carries Hazard.c4d's own text. QuakeR's weapon descriptions had "Schussmodus" and "kann" in English. The localization checker now splits string-table lines only at CR and LF, as `C4LangStringTable` does (`C4LangStringTable.cpp:54-59`), so a Windows-1252 ellipsis (0x85) no longer ends a line. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#75](https://github.com/clonk-org/clonk-rs-content/issues/75) | Collection: `Puzzles.c4f` and `Races.c4f` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the exact-copy decision recorded on #68 for #69–#78. Six definitions shipped only a `ScriptDE.c`, which `C4ComponentHost::LoadAppend` never loads for an English player (`C4ComponentHost.cpp:155-189`), so they had no script at all in English: the Super Mega Ultra tank, its wheels and entrance, both Clonk Quiz 3 joker banners and the Explorer's priming wire. They are `Script.c` now. Super Mega Ultra's description was a `Desc.rtf`, which no language loads (`C4ComponentHost.cpp:55-95`); it is `DescDE.rtf`, bytes unchanged. English selector descriptions that were missing (Clonk Quiz 3, Death Race, Super Mega Ultra), a placeholder (tschudispecial), copied from another scenario (Catapult Race said Sky Race, Downhill the Wrong Way said Downhill), cut short or saying something else were written from the German, and so were those of Tower of Magic, Tomb Raiders, Profanation of the Temple, both `_D_` temples, Mirror of Dreams, Brickrace, Race of the Magician Clonks and Wipf-o-drom. Clonk Quiz 3 and Super Mega Ultra got a `Title.txt`, and the English titles of the two `_D_` temples were the "New Scenario" placeholder. Object descriptions and names that were missing, placeholders, garbled or German were written from the German; the vines and cobwebs take the English Feindesland gave the same objects. Two localization files had non-canonical names (`StringtblUS.txt`, `DescUs.txt`), two `Names.txt` used the `EN:` code, which no language matches, both Explorers' English tables wrote `StartForschen:`, and the Tube Mail extractor's defined `SendObject` twice. English tables that lacked keys of their German twins got them (S2Christmas's quest log, inventory and tree ornaments, S2Jungle's helicopter and Parji, Wipf-o-drom's spectators), and Mirror of Dreams' dialogue and riddle and Wipf-o-drom's spectator log were written again from the German. The player text in the scripts moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables: Clonk Quiz 3's story, 30 questions and both fall guards, with its blimp, tower and joker captions; Brickrace's menus and announcements, using the keys its German table already held word for word; Profanation's difficulty menu; S2Jungle's load error; Mirror of Dreams' wheel, switches, inscription signs, hidden door and throne; the Mount Vikto lock, the Explorer's whip, and the Ropepack pulley and rope end; Super Mega Ultra's tank, horse, restart command, rules menu, finish and death messages; tschudispecial's intro; the start log of four races; S2Tower's village guard, princess and stargate level; S2Christmas's death menu; and Explode1000's one German death message. Seven switches captioned `[DescSwitch]` without the `$`, the S2Tower submarine's English table wrote `TxtSteuer = ` (C4LangStringTable keeps that space in the key, `C4LangStringTable.cpp:55-67`), and a stargate dialer asked for `$NoGate` without its closing `$`; all three showed the raw name. Mount Vikto's cauldron append, both Explorers and Race of the Magician Clonks asked for keys none of their tables had; those take the text of Western's cauldron, FarWorlds' Jungle Clonk and Caverace, which they copy. A spelling check over every English value, description and name of both folders then found English that was misspelled, garbled or still German: typos were corrected in place, and Mount Vikto's dialogue, Waterrace's description, warnings and crown messages, S2Jungle's description, the rope pack's object descriptions (which left out the controls) and single values in S2Jungle, S2Tower, Mirror of Dreams and Tower of Magic were written again from the German. Waterrace's death messages 4, 9 and 10 follow its own German rather than the Caverace text its English was copied from, and the Regeneration rule takes the English Feindesland gave the same German. The localization checker now also rejects a string-table key with whitespace around it, which C4LangStringTable keeps as part of the key; the 36 such keys in `Settling.c4f/M_Kathedral.c4s` are left to #77. Menu commands, `LocalN`/`Call` names and credits keep their bytes, and `Scriptnew.c`, which is not a script component, is untouched. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#152](https://github.com/clonk-org/clonk-rs-content/issues/152) | `Collection.c4f/Fun.c4f/ClonkPartyRemake8-clonk-rs-localized.c4s`: `Recycler.c4d/Throne2.c4d` and `Abwärts.c4d/Ropepack.c4d/Pulley.c4d` | the maintained derivative recorded above (CCAN entry 6111) | English localization in place, left over from #58. The throne's two functions and the pulley's `Activate` were captioned in German (`[Thron drehen]`, `[Menu öffnen]`), which English players saw in the context menu. Both captions moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables: the throne takes `Clonk_Knight_Party.c4s`'s key and English for the same caption (`CtrlTurnThrone`, "Turn throne"), and the pulley's key follows the `CPR_` prefix of its existing table. Credits, ISO-8859-1, CRLF and `$Key$` substitutions are preserved. |
| [clonk-org/clonk-rs-content#76](https://github.com/clonk-org/clonk-rs-content/issues/76), part 1 | Collection: the RufDerWipfeRE object pack `Settling.c4f/RufDerWipfeRE.c4f/RufDerWipfe.c4d` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, under the exact-copy decision recorded on #68 for #69–#78. All 205 German object descriptions that had no English one were translated from the German, the encyclopedic science texts included. 203 definitions whose only name was the German DefCore name got a `Names.txt` with that name and an English one; the coin's DefCore name had lost its umlaut (`Mnnze`), so its German line reads `Münze`. 29 `Names.txt` with only a German line got their English line, one used `EN:`, which no language matches, and 17 English names that were wrong or garbled were corrected, among them Kräuter as "Grass", Eiskugel as "Eisball", Gewehr as "Riffle", Kornbündel as "Korn" and Giftgas as "Poision gas". Eleven string tables with only a German side (the rule chooser, the tavern, the drinks, the uranium goal, the slingshot, the construction commands and others) got English twins line for line. Three scripts asked for keys none of their tables had, which C++ reported in every language: the slingshot post's controls now carry the cannon tower's German and English, the cooked monster egg its sibling foods' `TxtEat`, and the fertilizer barrel the `TxtEject` of its commented-out code, which substitution still reads. Seven `DescDe.txt` and one `StringtblDE.txt` got canonical names, and three garbled English descriptions ("objet", "enviroment", "aimable icestrike") were written again from the German. The pack's script text follows in a later part. Credits, ISO-8859-1, CRLF and `%`/`$Key$` substitutions are preserved. |
| [clonk-org/clonk-rs-content#76](https://github.com/clonk-org/clonk-rs-content/issues/76), part 2 | Collection: the scripts of the RufDerWipfeRE object pack `Settling.c4f/RufDerWipfeRE.c4f/RufDerWipfe.c4d` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place. The German player text in the pack's scripts, some 280 strings, moved verbatim into paired `StringTblDE.txt`/`StringTblUS.txt` tables beside each script (60 new pairs, the rest extended): function captions, messages, logs, menu entries and their infos, and message-window captions. Among them are the Effektor's 25 tips and its HUD and AI menus, the goals' settings menus, the wind report and its adjectives, the government messages, and the bank, village, laboratory, temple, smithy and tower-console menus. Only display positions moved. The government names `GetGovernment()` returns keep their bytes, because the peace rule, the Clonk's needs and the airport compare them; their display copies in `GetGovernmentLOKAL()` moved by exact snippet. Menu commands, action, sound, particle and graphics names, the cheat phrase the Effektor compares with player input, the hireable Clonks' names and the wind-storage object's generated script code keep their bytes too, and the disabled `.c4dx` definitions, which never load, are left alone. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#76](https://github.com/clonk-org/clonk-rs-content/issues/76), part 3 | Collection: the scenarios and folders of `Settling.c4f/RufDerWipfeRE.c4f` around the object pack | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, and two authored defects that only English players met. 27 German scenario and folder descriptions got a `DescUS.rtf` translated from the German, and Isles in the Sky's English description, which read "...", was written from the German. 18 scenario-local definitions got a `DescUS.txt`, and four English ones that were a placeholder ("A new object definition."), garbled or cut short were written again. 17 titles got their English line, and Narshe, which had no `Title.txt` and so showed its German core title in every language, got one. 15 definitions whose only name was the German DefCore name got a `Names.txt`, and three German-only `Names.txt` their English line. The German player text in the scenario scripts moved verbatim into paired tables beside each script: the 1Intro dialogue, the cook's dialogue in the scenario's `System.c4g` (which loads its own table, `C4Game.cpp:3346, 3353`), the tutorial's 46 messages and goal texts, the campaign's goal messages, captions and logs, the missions' god messages, ModernBattle's ticket logs, every scenario's signpost place names (2Teudoburger's keys `TxtPose1-3`/`TxtUnDef` are followed), and the wind-data log. ModernBattle's team names moved into its tables, which the engine applies to `Teams.txt` (`C4Game.cpp:230-235`, `C4Teams.cpp:651`). 1Intro's `Objects.txt` names four objects in German: the cook and his kitchen hand, who speak the cook's dialogue, a fish and a statue. The engine reads `Objects.txt` without a string table (`C4GameObjects.cpp:535-548`), so the scenario's `Initialize`, which runs after the objects are loaded (`C4Game.cpp:2712, 2733`), gives each object that carries one of those German names the table's name for the player's language; in German that is the same name. Garbled English table values were written again: 2Teudoburger's place names, two ScriptGoal messages, ModernBattle's damage and healing statistics and the airstrike message. The defects: 5Arallsee's pollution goal `UMZS` shipped only a `ScriptDE.c`, and LanguageEx is just the player's language (`C4Config.cpp:1466-1473, 1492-1507`), so an English player's goal had no script and never reported fulfilled (`C4RoundResults.cpp:296-299`), and the mission could not be won; it is `Script.c` now. 3Frangreich's plans asked for `TxtViewplans`, which no table had (`string table entry not found` in every language); both tables have it now, with the German that the orphaned `CtrlActivateDesc` carried. Strings the scripts compare keep their bytes: the German names that the rename compares with `Objects.txt`, and the intro's character table. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#77](https://github.com/clonk-org/clonk-rs-content/issues/77), part 1 | Collection: `Settling.c4f/M_Kathedral.c4s`, `Settling.c4f/DieNeueWelt.c4s` and `Settling.c4f/Clepler-38b.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, and English keys that never matched. M_Kathedral's residents had English tables with 36 keys padded with spaces around `=`, and C4LangStringTable keeps everything before the first `=` as the key (`C4LangStringTable.cpp:53-68`). They also had `MSG12`/`MSG13` where the scripts and German tables say `Msg12`/`Msg13`, and the lookup is the case-sensitive `SEqual`. So most of their English dialogue showed raw keys, and what did show was a poor machine translation. Their English tables, the scenario's messages, the alchemist's book, and the alchemy table's and glass furnace's captions were written again line for line from the German. DieNeueWelt's dialogue table (about 150 lines, in the scenario's `System.c4g`) and Clepler-38b's five German-only tables got English twins. Clepler's rules and transmitter tables got the keys they lacked (`CtxDoCollect`/`CtxDoNotCollect`, `MsgOccupied` and five transmitter texts). DieNeueWelt's section headings, which C++ skips because they carry no `=`, became `#` comments. German that only scripts held moved into tables beside them: the lock's and terraformer's captions, the jetbelts' "%s verloren!" and the residents' names. The temple switch captioned its action `[DescSwitch]`, which shows the key's name, where its tables define `DescSwitch`; it says `[$DescSwitch$]` now. M_Kathedral's `Objects.txt` names three residents in German ("Bewohner", "Berarbeiter", "Dorfbewohner"). The engine reads it without a string table (`C4GameObjects.cpp:535-548`), so the scenario's `Initialize` gives them the names of the player's language; in German those are the same. Missing, wrong, garbled and cut-short English descriptions, names and titles were written from the German: among them the cathedral and its plans, silver, the pan and the pickaxe, which all read "Very expensiv!", DieNeueWelt's settler, which carried the Clonk's description, and M_Kathedral's story. The base game's own English texts (the trees, the skeleton knight) were left as they are. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#77](https://github.com/clonk-org/clonk-rs-content/issues/77), part 2 | Collection: fifteen smaller `Settling.c4f` scenarios, `m0XeronSettlement.c4s`, `m0Arctic2.c4s`, `Q8Castle_1_1.c4s`, `(G)Deeep.c4s`, `Tholus.c4s`, `Goldwipfcaves.c4s`, `InsideOut.c4s`, `Decorace.c4s`, `Wipfenfels.c4s`, `Ragnaroek.c4s`, `DM2_Svenite.c4s`, `ColdMountains.c4s`, `Wipfberge.c4s`, `GoldabbauWes.c4s` and `Rustuff-Chasm.c4s`; `GoldenCanyon_2.c4s` (audited, unchanged) | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place, and English keys that never matched. Decorace's adornment table spelled its English keys with the `$` signs (`$ErrNoTree$=...`). C4LangStringTable keeps everything before the first `=` as the key (`C4LangStringTable.cpp:53-68`), so the English lookups never matched. Ragnaroek's English goal table had a fifth step that the script never shows, so its third and fourth steps described the wrong stage; it was written again line for line from the German. Tholus's research lab lacked the `ContinueCloning` key, and two of its seeds named one climate where the German and the script name two. m0Arctic2's campfire and cauldron got the English tables they lacked, and its elevator got the two keys it lacked. (G)Deeep's glass, seaweed and construction-kit append asked for keys that no table defined in either language, so both languages showed the raw keys. They got paired tables, with the glass captioned for the glass bridge it builds. German that only scripts held moved into tables beside them: (G)Deeep's introduction and building rule, Q8Castle's difficulty menu, m0Arctic2's summoning messages and two torches' message. m0XeronSettlement's goals and Tholus's revegetation goal spelled their tables `Stringtbl`. C++ matches group entry names ignoring case (`StdFile.cpp:337-354`), so the tables were renamed to the canonical spelling with no change at runtime. English that was missing, German, garbled or wrong was written from the German. This covers m0XeronSettlement's goal and spirit texts, m0Arctic2's spirits, and Wipfenfels's guards, one of whom said "Lets kill him!" for "get out of here and never show your face again". It also covers the wormhole goal, and descriptions, names and titles such as (G)Deeep's shells, its tarpoon (the English said it explodes; it does not), and five scenario descriptions. The base game's own translations (the tree population, the power plant, the Golden Canyon story and others) were left as they are. The exceptions are the settler, whose English is the Clonk's description, and copies whose German says something else. Credits, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#77](https://github.com/clonk-org/clonk-rs-content/issues/77), part 3 | Collection: the `Settling.c4f` story scenarios `(G)Empyrean.c4s`, `(G)Volc.c4s`, `Die_Flut_kommt_III.c4s` (with its nested `sensor.c4d` pack), `OriginalAdventure.c4s` and `TreasureOfTheAcidLake.c4s` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place. German that only scripts held moved into tables beside them: Empyrean's intro and act dialogue (about 110 lines), research help and team and temple messages; Volc's surveyor; Die_Flut_kommt_III's forest hut and scenario messages; OriginalAdventure's signposts and travel log; the acid lake's Hans, Gustav, plans and hints. English tables that were missing or held German were written from the German: Die_Flut_kommt_III's sign and sensor pack, and OriginalAdventure's travel and snow-golem lines. Keys that no table defined in either language got paired tables (OriginalAdventure's section helpers, Empyrean's clonk append and air spirit). OriginalAdventure's German `System.c4g` table defined `DlgVyr17` twice with the same text; C++ keeps the first match, so the second was dropped. Die_Flut_kommt_III's `title.txt` was renamed to the canonical `Title.txt`; C++ matches entry names ignoring case (`StdFile.cpp:337-354`). Three script defects that showed a raw token or worked in one language only were fixed. The forest hut left the four disaster triggers out of its tree menu by their German names only, so English players were offered them as trees; their IDs now keep them out in every language. The flood's rock rain wrote `$Rock` without its closing `$`, which C4LangStringTable skips as no valid key (`C4LangStringTable.cpp:74-86`), so the raw token was the rain's name. The acid lake's goal message read `%d % still` where the German and the call pass `%d %s`. Poor, garbled and wrong English was rewritten from the German, including Volc's machine-translated story, the flood's settings ("Shell there be lightnings?", a goal message naming the wrong goal and amount) and OriginalAdventure's wizard, whom the English called Roy where the script names him Daniel. The base game's own translations were left as they are (the castle, the frost wave, the golems and others). Credits, including the sensor pack author's contact line, ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#77](https://github.com/clonk-org/clonk-rs-content/issues/77), part 4 | Collection: the exploded `Settling.c4f` folders `TropicalWorlds.c4f`, `NC_81__s_Selfmademaps.c4f`, `HazardousWorlds.c4f` and `HostileWorlds.c4f`; with it `Collection.c4f` is no longer pending on #77 in `packs.toml` | `UltimateClonkCompilation-v3.0.zip` tree recorded above, as imported in `b9214caf` | English localization in place. The selector prose of all four folders was rewritten from the German where it was machine-translated or misspelt: the three TropicalWorlds scenarios ("Your explorer brings up anger"), NC_81's sky treasure ("the healing unicellular skull") and all ten HazardousWorlds descriptions. NC_81's missing folder and scenario descriptions were added, and so was a `Title.txt` for `Sandwüste.c4s`, whose German `[Head]` title English players saw (`C4Game.cpp:253-255`). The scenario's directory keeps the imported name with U+FFFD in place of the umlaut, the non-UTF-8 name caveat of #68. German that only scripts held moved into tables beside them: the jungle dealer Novo's dialogue (in his broken English, as his German is broken), NC_81's three scenario intros, a torch's message and Arctic Depth's building rule. Keys no table defined got tables (the joint and the seaweed). Three `Stringtbl` pairs were renamed to the canonical case with `git mv`; C++ matches entry names ignoring case (`StdFile.cpp:337-354`). Titles were corrected ("Acidcliffs", "Metaldesert", "Volcan Isle", "Coral Skys"). Missing, placeholder and wrong names and descriptions were written from the German, including the tropical shells, NC_81's idol and rocket, and copies whose German differs from the base game's (the death crow). The base game's own translations were left as they are (the polar lights, the alcohol effect, the holy statue). Credits (Gecko, Christoph Süß), ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#78](https://github.com/clonk-org/clonk-rs-content/issues/78) | `ClonkMars.c4d`, `ClonkMars.c4f` and `E.P.I.C.c4f`; with it the three packs are no longer pending on #78 in `packs.toml` | ClonkMars: the `c90bafba` imported tree above, plus `2cb2a7bd` and `2772c602`. E.P.I.C.: the maintainer snapshot above, as moved in `b9214caf` | English localization in place, under the exact-copy decision recorded on #68 for #69–#78. ClonkMars' own English was kept where it reads. Where it was machine-translated, garbled, cut short or wrong, it was rewritten from the German. That covers six of the seven selector descriptions (Elysium Abyss kept the German "zugefunkt"; Phobos Station read "The first teams on Mars have been colonized") and the capsule, which dropped its controls. It also covers the accumulator ("Threw --> decharge"), base, lab, material unit, splitter ("shivers") and tank, the space pilot, the transmitter's captions (as in Clepler-38b), and names such as "Abadoned Base" and "Supportcapsule". The rule folder's `Names.txt` had the two languages the wrong way round. The power line had only an English table, so a German player whose line broke got an empty message: `Message` takes a `$…$` it is handed for a sound name and shows only the text before the first `$` (`C4Script.cpp:2420-2431`). That is the `TxtLinebroke` warning the #77 part 1 probe found; it got a German one. The English system table's `Pipe` was the German "Rohr". German that only scripts held moved into tables: the LED's message and E.P.I.C.'s death and relaunch logs. E.P.I.C.'s two effect helpers got the `Kill` key their scripts ask for. Two `Stringtbl` pairs were renamed to the canonical case with `git mv`; C++ matches entry names ignoring case (`StdFile.cpp:337-354`). The packs' jokes and base-game texts were kept, as were the German developer diagnostics ("ERROR: HUD: …"). E.P.I.C. is written in English and eight of its scripts have only an English table; that German-side gap is outside this English localization and is tracked in [clonk-org/clonk-rs-content#162](https://github.com/clonk-org/clonk-rs-content/issues/162). Credits (Nachtfalter, Luchs, jsb), ISO-8859-1, CRLF and `%`/`$Key$`/`{{ICON}}` substitutions are preserved. |
| [clonk-org/clonk-rs-content#162](https://github.com/clonk-org/clonk-rs-content/issues/162) | `E.P.I.C.c4f`: the construction-kit and tree appends, the Clonk overload, the apothecary, the comet silo, the packed storm glider and the toolbelt in `E.P.I.C.c4d`, and `Wound_Valley_King.c4s/Throne Room.c4d` | The maintainer snapshot above, as moved in `b9214caf` and localized under [clonk-org/clonk-rs-content#78](https://github.com/clonk-org/clonk-rs-content/issues/78) | German string tables. E.P.I.C. is written in English, and these eight scripts name keys that only an English table defined. A German player's default language sequence is just `DE` (`C4Config.cpp:1472-1473`, `1492-1507`), so the engine found no table. Menus showed the raw keys, such as `$CtxChop$` in the Clonk's context menu, and messages built on a key showed nothing: `Message` and `PlayerMessage` take a `$…$` for a sound name and show only the text before the first `$` (`C4Script.cpp:2400-2431`). Each gets a `StringTblDE.txt`, line for line from its English table. Where the base game pairs the same key with the same English, its German is reused: the construction kit, tree, Clonk, chemical factory, alchemy bag and wizard tower. The throne room's table is the wizard tower's. The toolbelt is the alchemy bag made a belt, so the bag's "Beutelinhalt" becomes "Inhalt" and "Alchemiebeutel" becomes "Werkzeuggürtel". E.P.I.C.'s own entries are translated from its English: "Totstellen", "Aufstehen", "Verstecken" and "Gleiter aufbauen". The three English-only tables no script reads (the loam and metal appends and the storm glider) are left alone. E.P.I.C.'s item names and descriptions stay English, as authored. ISO-8859-1, CRLF and `%` substitutions are preserved. |

For every future derivative change, append a row naming its pull request or
commit, affected scope, source baseline, exact purpose, and preserved notices.
If it comes from a newer upstream artifact, also update the source ledger with
the artifact filename, source URL or reference, version, SHA-256 and hash scope.

The `localization` lists in `packs.toml` are temporary audit debt, not a rights or
modification allowlist. Removing a row makes that scope subject to the complete
localization audit regardless of its `binary` attribute.
